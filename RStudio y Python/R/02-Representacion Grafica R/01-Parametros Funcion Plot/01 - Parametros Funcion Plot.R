#Log para indicar que queremos escala logaritmica
#main("Titulo") Para añadir titulo al grafico
#expression() para poner una expresion matematica

#xlab("etiqueta") o ylab("etiqueta") para poner etiqueta en cada eje

#pch=n para cambiar la forma de los puntos n =1-25
#cex Para elegir el tamaño de los simbolos 
#col="Color en ingles"
#Mirar el github del profe para ver todos los colores de R

#F[n], para escribir la expresion
#log = "xy"

#Para colocar un grafico al lado de otro par(mfrow = c(1,2))
#Esto anterior se puede hacer antes de cualquier plot
#y al final se debe poner par(mfrow=c(1,1))


#{r fig.align='center', echo = FALSE}